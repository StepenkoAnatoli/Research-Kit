# Evidence

| ID | Retrieved | Type | URL | Finding | Raw |
|---|---|---|---|---|---|
| E-01 | 2026-09-22 | P | https://nodejs.org/api/single-executable-applications.html | **The postject step is gone, and the page dates it.** "Added built-in single executable application generation via the CLI flag `--build-sea`", under **v25.5.0**, with a dedicated section "Generating single executable applications with `--build-sea`" and the invocation `node --build-sea sea-config.json`. So producing a binary no longer needs an external injector installed and pinned alongside Node. Graded P; it is the same page E-01 of the sea-assets corpus cites, re-fetched today for a different question. | research/raw/2026-09-22-single-executable-applications-node-js-v-nodejs-acca3287.md |
| E-02 | 2026-09-22 | S | https://docs.deno.com/api/node/sea/ | **A second runtime documenting Node's `node:sea` module - and what it does NOT say is the finding.** Deno implements Node compatibility and documents the same API surface, including `getAsset` and `getAssetAsBlob` ("Similar to `sea.getAsset()`, but returns the result in a Blob"). That independently confirms the assets API the sea-assets corpus turns on. It contains **no mention of `--build-sea`**, which is expected rather than contradictory: `--build-sea` is a Node CLI flag for producing a Node binary, and a compatibility layer documents the runtime module, not the other runtime's build tooling. Recorded because a reader comparing the two pages would otherwise wonder. | research/raw/2026-09-22-sea-node-documentation-deno-docs-deno-2d194112.md |
| E-03 | 2026-09-22 | S | https://github.com/orgs/nodejs/discussions/4569 | **Carries nothing, and this time the gate said so before a human did.** "Creating multi-platform/arch Single Executable Applications (SEA)" is a GitHub Discussion, whose body renders in the browser, so the capture holds page furniture and the title. 8 KB against the Node page's 42 KB, and **six render-failure notices** - which is `capture-completeness/partial-render` firing correctly on a collection it had nothing to do with (ADR-0037). Graded S, cited by no unknown, kept because the ledger records the fetch. [render-reviewed: searched the capture for the discussion body and confirmed it is absent - the notice IS the finding here, nothing is cited from this row, and the only text it carries is the thread title quoted above] | research/raw/2026-09-22-creating-multi-platform-arch-single-exec-github-a4fdafbf.md |

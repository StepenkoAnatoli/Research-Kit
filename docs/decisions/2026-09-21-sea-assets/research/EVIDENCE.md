# Evidence

One row per fetched page. `Raw` points at the cached page text under `research/raw/`,
which is what makes the claim checkable - a row without raw evidence fails preflight.

| ID | Retrieved | Type | URL | Finding | Raw |
|---|---|---|---|---|---|
| E-01 | 2026-09-21 | P | https://nodejs.org/api/single-executable-applications.html | **The deferral in ADR-0031 rested on a false premise, and this page says so plainly.** Assets can be bundled without `useVfs`: "Users can include assets by adding a key-path dictionary to the configuration as the `assets` field. At build time, Node.js would read the assets from the specified paths and bundle them into the preparation blob", retrieved in the executable through `sea.getAsset()`, `sea.getAssetAsBlob()`, `sea.getRawAsset()` and `sea.getAssetKeys()`. **The stability picture is the finding.** The page carries "Stability: 1.1 - Active development" at its head, and `1.0 - Early development` appears exactly once in the whole document - under "Virtual file system (VFS) for assets", added in v26.9.0. The assets API carries no separate marker and therefore sits at the feature's 1.1. So `useVfs` is a CONVENIENCE - it lets existing `node:fs` calls keep working unchanged - and not the only route. A kit willing to branch on `sea.isSea()` and call `getAsset()` reaches its templates, recipes and schemas at 1.1 throughout. Documents Node.js v26.9.0. | research/raw/2026-09-21-single-executable-applications-node-js-v-nodejs-acca3287.md |
| E-02 | 2026-09-21 | S | https://lira.epac.to/DOCS/nodejs/api/single-executable-applications.html | **A stale mirror, kept because of what it demonstrates rather than what it says.** This is the same Node.js document as E-01, republished on an unrelated host - and it documents **v20.19.2**, six major versions behind the v26.9.0 that nodejs.org serves. It predates the VFS section entirely, so a reader who found it first would conclude the assets API is the only option and never learn there is a newer one. **Why the row is kept:** the `corroboration` check added to this kit hours earlier counts distinct HOSTS, so citing E-01 and E-02 together for one unknown would be reported as `independent` - two hosts, apparently two witnesses. They are one document, one of them stale. That is a sharper failure of the host heuristic than ADR-0036 anticipated: a mirror does not merely fail to add support, it looks like support while being older. Cited by no unknown, deliberately. | research/raw/2026-09-21-single-executable-applications-node-js-v-epac-a43bc035.md |
| E-03 | 2026-09-21 | S | https://github.com/nodejs/single-executable/discussions/17 | **Carries nothing.** The discussion body is loaded by script, so the capture holds GitHub page furniture, a sponsor block, a star count and the thread title - "Bundling of non-binary assets within the binaries" - and none of the conversation. The title alone confirms the topic was discussed by the Node.js single-executable group and proves nothing about the outcome. Recorded because the fetch happened and the ledger says so, and graded S and cited by no unknown because an empty page supports nothing. The completeness grade reads `full`, which is honest about the bytes returned and not about the content - a limit of grading transport rather than meaning. | research/raw/2026-09-21-bundling-of-non-binary-assets-within-the-github-f624b1a0.md |

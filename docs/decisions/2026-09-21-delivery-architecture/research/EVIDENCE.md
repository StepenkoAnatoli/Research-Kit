# Evidence

One row per fetched page. `Raw` points at the cached page text under `research/raw/`,
which is what makes the claim checkable - a row without raw evidence fails preflight.

The `Finding` cell arrives as an auto-extracted summary. Rewrite it into a real claim:
what the page actually establishes, with the quote or number that proves it.

| ID | Retrieved | Type | URL | Finding | Raw |
|---|---|---|---|---|---|
| E-01 | 2026-09-21 | P | https://docs.github.com/en/rest/actions/workflows | **The dispatch endpoint returns the run it created, under a current API version.** "Create a workflow dispatch event" documents exactly one status code - `200`, "Response including the workflow run ID and URLs" - and the example body is `{"workflow_run_id": 1, "run_url": "...", "html_url": "..."}`, rendered at `apiVersion=2026-03-10`. This CONTRADICTS a finding recorded against this repository on the same day, which observed `204 No Content` and concluded the endpoint returns no identifier at all. Both are correct and the difference is the API version, verified on this machine rather than inferred: the same dispatch against `StepenkoAnatoli/Research-Kit` returned `HTTP/2.0 204 No Content` with `X-Github-Api-Version-Selected: 2022-11-28`, and `HTTP/2.0 200 OK` with `X-Github-Api-Version-Selected: 2026-03-10` and the body `{"workflow_run_id":35548135379,"run_url":"...","html_url":"..."}`. Run `35548135379` then resolved to a real queued `workflow_dispatch` run, number 50, created `2026-09-21T00:34:37Z`. The page also states the permission this needs: "Actions" repository permissions (**write**), and a maximum of 25 input properties. | research/raw/2026-09-21-rest-api-endpoints-for-workflows-github--github-0e373307.md |
| E-02 | 2026-09-21 | P | https://docs.github.com/en/actions/concepts/workflows-and-actions/workflow-artifacts | Artifacts are delivery, not storage, and the page says so by what it ties them to: an artifact exists to "persist data after a job has completed", and "When a workflow run is deleted all artifacts associated with the run are also deleted from storage" - deletable through the UI, the REST API or `gh run delete`. It also draws the line this project needed drawn: use caching for files "that don't change often between workflow runs", artifacts for files "to use or view after a workflow run has ended". A collected corpus is the second, not the first. This page carries **no retention figure**; the number lives on the repository-settings page at E-03, which is why both were collected. | research/raw/2026-09-21-workflow-artifacts-github-docs-github-4185fd28.md |
| E-03 | 2026-09-21 | P | https://docs.github.com/en/repositories/managing-your-repositorys-settings-and-features/enabling-features-for-your-repository/managing-github-actions-settings-for-a-repository | **Artifacts expire, and the ceiling depends on repository visibility.** "By default, checks, workflow runs, commit statuses, and the artifacts and log files generated by workflows are retained for 90 days before they are automatically deleted." The adjustable range is asymmetric: "For public repositories: you can change this retention period to anywhere between 1 day or 90 days. For private repositories: you can change this retention period to anywhere between 1 day or 400 days." A change "only applies to new" objects and is not retroactive, and a single artifact may carry its own shorter period. One dated caveat worth carrying into any design that outlives it: "Starting October 1, 2026, these policies will apply to checks, workflow runs, and commit statuses in addition to artifacts and logs. Until then, checks, workflow runs, and commit statuses are retained for 400+ days regardless of your configured retention period." So on this public repository a ZIP is guaranteed for at most 90 days, and cannot be extended by configuration without going private. | research/raw/2026-09-21-managing-github-actions-settings-for-a-r-github-80e052e4.md |
| E-04 | 2026-09-21 | P | https://docs.github.com/en/billing/concepts/product-billing/github-actions | **Runner time is free here and metered if this repository goes private.** "GitHub Actions usage is **free** for **self-hosted runners** and for **public repositories** that use standard GitHub-hosted runners." For private repositories each account gets a monthly quota: GitHub Free 2,000 minutes and 500 MB artifact storage; Pro 3,000 and 1 GB; Team 3,000 and 2 GB; Enterprise Cloud 50,000 and 50 GB. Two rules decide who pays: "Minutes usage is charged to the repository owner, not the person who triggered the workflow runs", and "Anyone with write access to a repository can run actions. Any costs of running the actions are billed to the repository owner." Storage accrues hourly in GB-Hours and is shared with GitHub Packages. Note the scope of "free": it is GitHub's runner minutes. It is not the Firecrawl credits this collector spends, which are metered on every route and are the real budget. | research/raw/2026-09-21-github-actions-billing-github-docs-github-74520ad8.md |
| E-05 | 2026-09-21 | P | https://nodejs.org/api/single-executable-applications.html | **Single-executable applications are "Stability: 1.1 - Active development", and Windows is supported.** The supported platform list is explicit, and the exclusion is not Windows: "Windows; macOS (arm64 only; x64 is not currently supported and is skipped in the tests); Linux (all distributions supported by Node.js except Alpine and all architectures except s390x)." So the local `.exe` option is buildable today for this operator's platform; the unsupported platform is macOS x64, which is out of scope. Two things temper it. First, 1.1 means the interface may still change under a shipped product. Second, the feature is visibly still moving: `--build-sea` was added in v25.5.0, and the bundled-asset virtual filesystem (`useVfs`) is newer still at v26.9.0 and carries its own weaker "Stability: 1.0 - Early development". The kit reads its own files at runtime, so the VFS path is the one it would depend on - and that is the least stable part of the feature. | research/raw/2026-09-21-single-executable-applications-node-js-v-nodejs-acca3287.md |
| E-06 | 2026-09-21 | P | https://docs.github.com/en/actions/how-tos/deploy/configure-and-manage-deployments/manage-environments | **An approval gate exists, and on a free plan it exists only while the repository is public.** Environments take deployment protection rules: "Required reviewers" - "Enter up to 6 people or teams. Only one of the required reviewers needs to approve the job for it to proceed", with an optional "Prevent self-review" - and a "Wait timer" in minutes; administrators' bypass can be disallowed. Environment secrets sit behind those rules: a job "can only access these secrets after any configured rules (for example, required reviewers) pass". The plan clause is the finding that changes the design: "If you are on a GitHub Free, GitHub Pro, or GitHub Team plan, other deployment protection rules, such as a wait timer or required reviewers, are only available for public repositories" - and, harder still, "Users with GitHub Free plans can only configure environments for public repositories. If you convert a repository from public to private, any configured protection rules or environment secrets will be ignored." Ignored, not refused. A free-plan repository that goes private to protect its research subject silently loses both the approval gate and the environment secret that holds the paid credential. Also: "Running a workflow that references an environment that does not exist will create an environment with the referenced name", with no protection rules on it. | research/raw/2026-09-21-managing-environments-for-deployment-git-github-0457ac44.md |
| E-07 | 2026-09-21 | P | https://docs.github.com/en/actions/concepts/security/secrets | **The credential is protected; the research subject is not.** Secrets "use Libsodium sealed boxes, so that they are encrypted before reaching GitHub", are injected only into a workflow that explicitly names them, and are redacted from logs - with a limit stated plainly rather than hidden: "Because there are multiple ways a secret value can be transformed, this redaction is not guaranteed. Additionally, the runner can only redact secrets used within the current job." Environment secrets can be put behind required reviewers. What the page establishes for this decision is the boundary: the protection is for **secrets**, a category a URL, a search query, a topic name or an evidence excerpt does not belong to. Nothing here protects those, and the anonymous probe recorded at E-08 shows what happens to them on a public repository. | research/raw/2026-09-21-secrets-github-docs-github-0f8d7aea.md |
| E-08 | 2026-09-21 | P | https://docs.github.com/en/actions/reference/workflows-and-actions/events-that-trigger-workflows | **`workflow_dispatch` is a real button with a real input contract, and one deployment constraint.** "This event will only trigger a workflow run if the workflow file exists on the default branch", and the Run workflow button appears only then; after one run it can be dispatched against any branch or tag through the API or CLI. Inputs are declared in the workflow file with `description`, `required`, `default` and `type` (including `choice` with `options`), are read through the `inputs` context, and are bounded: "The maximum number of top-level properties for `inputs` is 25" and "The maximum payload for `inputs` is 65,535 characters". The `inputs` context preserves Booleans where `github.event.inputs` stringifies them. Measured alongside this page, unauthenticated, against this public repository - so it is a property of the platform, not a reading of it: an anonymous caller GETs run metadata (`200`), the job listing with step names (`200`), and the artifact listing (`200`); `/actions/secrets` returns `401`, and raw job-log download returns `403 "Must have admin rights to Repository."` So on a public repository the run's *shape* - workflow name, job and step names, artifact names - is world-readable while secrets and raw logs are not. Artifact and step names are enough to disclose a research topic. | research/raw/2026-09-21-events-that-trigger-workflows-github-doc-github-bbcb15f7.md |
| E-09 | 2026-09-21 | P | https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-syntax | **A run can be named from a dispatch input, so a caller-supplied token can be made visible on the run itself.** `run-name` "is the name for workflow runs generated from the workflow", shown in the Actions tab, and "This value can include expressions and can reference the `github` and `inputs` contexts" - the documented example being `run-name: Deploy to ${{ inputs.deploy_target }} by @${{ github.actor }}`. This is the mechanism a correlation token would use if one were needed. Given E-01, one is not needed for a caller that pins `X-GitHub-Api-Version: 2026-03-10` and reads `workflow_run_id` from the response; it remains the fallback for a caller stuck on `2022-11-28`, and it costs one input and one line. | research/raw/2026-09-21-workflow-syntax-for-github-actions-githu-github-ce43d72d.md |
| E-10 | 2026-09-21 | P | https://docs.github.com/en/rest/about-the-rest-api/api-versions | **Corroborates E-01 from an independent page, and explains the 204 rather than restating it.** The sentence that settles it: "Requests without the `X-GitHub-Api-Version` header will default to use the `2022-11-28` version." So the unpinned call that returned `204 No Content` was not served by a broken endpoint - it was served by a three-year-old API version, exactly as designed. The page also classifies the change: "Removing or renaming a response field" is listed among breaking changes, and "Breaking changes will be released in a new API version", which is why `200` with `workflow_run_id` and `204` with nothing can both be correct at the same moment. **A fact E-01 did not carry, and it has a deadline.** Support is "at least 24 more months following the release of the new API version", signalled by `Deprecation` and then `Sunset` headers - the same two this repository observed on its own 204 response (`Deprecation: Tue, 10 Mar 2026`, `Sunset: Fri, 10 Mar 2028`). After that: "Requests that specify a closing down API version receive a `410 Gone` response. Requests that do not specify an API version default to the **next oldest supported version**, not the closing down version. If you rely on unversioned requests, you may observe behavioral changes as older versions are removed from support." An unpinned client therefore does not keep working - it silently changes behaviour on a date. That converts ADR-0031's "pin the version" from a preference into a dated requirement. | research/raw/2026-09-21-api-versions-github-docs-github-11e621e6.md |
| E-11 | 2026-09-21 | P | https://docs.github.com/en/actions/reference/workflows-and-actions/deployments-and-environments | **Corroborates E-06 and states the free-plan rule more directly than E-06 did.** E-06 said a Free repository converted to private has its protection rules and environment secrets "ignored". This reference page says the same thing as availability rather than as consequence, per feature: "If you are on a GitHub Free, GitHub Pro, or GitHub Team plan, **required reviewers** are only available for public repositories"; the identical note for **wait timers**; the same for **administrator bypass** and for **custom deployment protection rules**. And for the one that matters most here: "If you are using GitHub Free, **environment secrets are only available in public repositories.** For access to environment secrets in private or internal repositories, you must use GitHub Pro, GitHub Team, or GitHub Enterprise." **The fact neither E-06 nor ADR-0035 had.** Environment **variables** are governed separately and no less strictly: "Environment variables are available for all public repositories. For users on GitHub Pro or GitHub Team plans, environment variables are also available for private repositories." So a Free repository that went private would lose not only `FIRECRAWL_API_KEY` but also `RESEARCH_KIT_COLLECTION_ENV` - the marker `collect.yml` uses to prove the protected environment exists at all. The workflow would refuse at the environment check rather than at the credential check, which is a better failure than silently spending, and a more confusing one than either ADR predicted. Also confirms the scoping this design leans on: "Secrets stored in an environment are only available to workflow jobs that reference the environment." | research/raw/2026-09-21-deployments-and-environments-github-docs-github-df3ed9f2.md |
| E-12 | 2026-09-21 | P | https://docs.github.com/en/actions/how-tos/write-workflows/choose-where-workflows-run/choose-the-runner-for-a-job | **Corroborates E-04, and is stronger than it.** E-04 said Actions usage is "free ... for public repositories that use standard GitHub-hosted runners". This page, which E-04 itself links to, says: "Use of the standard GitHub-hosted runners is **free and unlimited** on public repositories." Unlimited is the word E-04 did not use, and it is the one that matters for a collector somebody might run often: there is no minute quota to exhaust on this repository, so the only meter is the Firecrawl credit. It also supplies what neither billing page did - the actual machines. `ubuntu-latest` and `windows-latest` are both 4 CPU, 16 GB RAM, 14 GB SSD, x64; `ubuntu-slim` is a single-CPU container on a shared VM rather than its own VM. The collector runs a Node process and one CLI, so the standard runners are ample and the choice between them stays a platform question rather than a capacity one. | research/raw/2026-09-21-choosing-the-runner-for-a-job-github-doc-github-36b41986.md |
| E-13 | 2026-09-21 | P | https://github.blog/changelog/2026-02-19-workflow-dispatch-api-now-returns-run-ids/ | **The mechanism behind U-1, from the team that shipped it - and it reads like a contradiction until you measure it.** "Previously, this endpoint returned only a `204 No Content` status code. Now, you can pass in a new optional boolean parameter, `return_run_details`, which will return a `200 OK` response containing the workflow ID, API URL, and workflow URL. If you do not pass in this parameter, it will continue to return the current `204 No Content` status code." Dated 2026-02-19. Read alone that denies E-01: this repository saw `200` with no such parameter. Both are true, and the seam is the API version - see the four-call measurement in the BRIEF. The lasting value is a **second route**: `return_run_details=true` works on `2022-11-28`, which is strictly better than the `created_at` polling ADR-0031 offered a pinned-out caller. It also dates the capability, which E-01 could not. | research/raw/2026-09-21-workflow-dispatch-api-now-returns-run-id-github-8d77d4ac.md |
| E-14 | 2026-09-21 | P | https://github.blog/changelog/2026-03-12-rest-api-version-2026-03-10-is-now-available/ | **Corroborates the versioning rule behind U-1 from the announcement side.** `2026-03-10` is "the **first calendar version to include breaking changes**", and "non-breaking changes (e.g., new endpoints, optional parameters, additional response fields) continue to be available across all supported API versions." That sentence is what reconciles E-13 with E-01: an optional parameter is non-breaking and reaches every version, while making it the DEFAULT changes a response shape and therefore needs a new calendar version. Different host and different document from E-10, which carried the same rule as reference prose rather than as a release note. | research/raw/2026-09-21-rest-api-version-2026-03-10-is-now-avail-github-6ae980eb.md |
| E-15 | 2026-09-21 | P | https://github.com/actions/upload-artifact | **Second source for U-2, written by the action's own maintainers rather than the docs team.** "The retention period must be between 1 and 90 inclusive", with `retention-days` documented as an input defaulting to repository settings. Confirms the 1-90 bound E-03 states for a public repository, from a different host and a different document - and it is the page an operator actually reaches when writing the upload step, which is where the bound gets hit in practice. | research/raw/2026-09-21-github-actions-upload-artifact-github-github-0fad02c7.md |
| E-16 | 2026-09-21 | P | https://github.com/pricing | **Partial corroboration of U-3, and the missing half is stated rather than glossed.** The pricing page lists "2,000 CI/CD minutes/month" on Free, "3,000" on Team and "50,000" on Enterprise, which independently confirms the PRIVATE-repository quota half of E-04 on a different host. **It does not state that public repositories are free**, which is the half U-3 actually turns on. So U-3 is corroborated where the money is metered and remains single-sourced where it is not - the opposite of what a row count would suggest, and a reason to read findings rather than tallies. [render-reviewed: searched the capture and confirmed present - the "2,000 CI/CD minutes/month", "3,000" and "50,000" lines this row cites. The failed widgets are pricing-page interactive elements, not the plan tables] | research/raw/2026-09-21-pricing-plans-for-every-developer-github-github-962f4f84.md |
| E-17 | 2026-09-21 | P | https://github.blog/changelog/2020-12-15-github-actions-environments-environment-protection-rules-and-environment-secrets-beta/ | **Second source for U-5, and it confirms the ORDERING that makes the gate worth anything.** "The required reviewers environment protection rule will automatically pause a job trying to deploy to the protected environment and notifies the reviewers." A job that is paused before it runs cannot have read the environment secret, which is the property ADR-0033 depends on when it puts `FIRECRAWL_API_KEY` behind the environment. E-06 states the rules exist; this states that they interpose before execution. Different host, different document, dated 2020-12-15. | research/raw/2026-09-21-github-actions-environments-environment--github-b7e00320.md |
| E-18 | 2026-09-21 | P | https://github.blog/changelog/2020-07-06-github-actions-manual-triggers-with-workflow_dispatch/ | **Second source for U-7, from the announcement that introduced the trigger.** "You will then see a 'Run workflow' button on the Actions tab, enabling you to easily trigger a run", and inputs are optional and rendered "as form elements in the UI". Corroborates the half of U-7 that decides whether "the operator clicks a button" is real. It does **not** speak to WHO may click it - the write-access requirement is still E-08 alone - so this row corroborates the mechanism and not the permission. | research/raw/2026-09-21-github-actions-manual-triggers-with-work-github-9b495286.md |
| E-19 | 2026-09-21 | P | https://github.blog/changelog/2022-09-26-github-actions-dynamic-names-for-workflow-runs/ | **Second source for U-8.** "The new `run-name` feature will accept expressions and be displayed on the list of workflow runs." That is the whole of what U-8 asked, from a different host and a different document than E-09, dated 2022-09-26. | research/raw/2026-09-21-github-actions-dynamic-names-for-workflo-github-0ce2ca70.md |
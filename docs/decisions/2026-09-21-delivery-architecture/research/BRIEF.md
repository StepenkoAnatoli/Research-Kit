# Brief - How Research-Kit should be delivered to a non-technical user

_Auto-drafted 2026-09-21 by `bin/brief.mjs` from the corpus. Sections marked **TODO**
require human/agent judgement; everything else is assembled from evidence already
in `research/`. While a **TODO** remains, this brief is **not reviewed** and the
handoff is **not approved** - a structurally valid corpus, a reviewed one, and an
approved handoff are three different states._

**This is the phase-1 to phase-2 handoff.** **Gate: PASS.** Every blocking unknown is closed with evidence, and every claim below
traces to a cached page in `research/raw/`.

Whoever you are - another agent, a different model, or a person - read this file
first. You should not need to re-research anything to start work. If something
here is not enough to build from, say which fact is missing rather than guessing
it: that is a phase-1 gap to close, not a phase-2 judgment call.

## Intent

Decide how Research-Kit reaches an operator who does not use a terminal, and record the
decision with evidence behind it rather than with a recommendation behind it. The four
candidate shapes are: a **GitHub Actions collector** the operator triggers from a web page
and downloads a ZIP from; a **local Windows `.exe`** built with Node's single-executable
feature; a **hybrid** that collects remotely and does the judgement locally; and a
**REST/API seam** exposing the kit to some other front end.

"Done" for this project is not a built delivery mechanism. It is a brief a builder can
implement from without re-researching the platform: what the dispatch API actually returns,
how long an artifact survives, what a run costs, what leaks when the repository is public,
whether an approval can gate a paid run, and whether Node can package this kit as an `.exe`
on the operator's platform at all.

**Why this project exists at a nested path.** It is the worked example of a convention this
repository adopted on 2026-09-21: research supporting a repository-level architectural
decision lives in a self-contained project at `docs/decisions/<date>-<decision-name>/`.
The convention exists because the first attempt at this research was collected into a
temporary scratch directory, read, and then deleted along with its captures, its ledger and
its provenance - leaving a recommendation with nothing under it. See
`../../../docs/preliminary-2026-09-21-delivery-architecture.md`, which is that note, kept
and labelled rather than quietly promoted.

## What we verified

| Claim | Source | Type |
|---|---|---|
| **The dispatch endpoint returns the run it created, under a current API version.** "Create a workflow dispatch event" documents exactly one status code - `200`, "Response including the workflow run ID and URLs" - and the example body is `{"workflow_run_id": 1, "run_url": "...", "html_url": "..."}`, rendered at `apiVersion=2026-03-10`. This CONTRADICTS a finding recorded against this repository on the same day, which observed `204 No Content` and concluded the endpoint returns no identifier at all. Both are correct and the difference is the API version, verified on this machine rather than inferred: the same dispatch against `StepenkoAnatoli/Research-Kit` returned `HTTP/2.0 204 No Content` with `X-Github-Api-Version-Selected: 2022-11-28`, and `HTTP/2.0 200 OK` with `X-Github-Api-Version-Selected: 2026-03-10` and the body `{"workflow_run_id":35548135379,"run_url":"...","html_url":"..."}`. Run `35548135379` then resolved to a real queued `workflow_dispatch` run, number 50, created `2026-09-21T00:34:37Z`. The page also states the permission this needs: "Actions" repository permissions (**write**), and a maximum of 25 input properties. | E-01 `docs.github.com` | P |
| **Artifacts expire, and the ceiling depends on repository visibility.** "By default, checks, workflow runs, commit statuses, and the artifacts and log files generated by workflows are retained for 90 days before they are automatically deleted." The adjustable range is asymmetric: "For public repositories: you can change this retention period to anywhere between 1 day or 90 days. For private repositories: you can change this retention period to anywhere between 1 day or 400 days." A change "only applies to new" objects and is not retroactive, and a single artifact may carry its own shorter period. One dated caveat worth carrying into any design that outlives it: "Starting October 1, 2026, these policies will apply to checks, workflow runs, and commit statuses in addition to artifacts and logs. Until then, checks, workflow runs, and commit statuses are retained for 400+ days regardless of your configured retention period." So on this public repository a ZIP is guaranteed for at most 90 days, and cannot be extended by configuration without going private. | E-03 `docs.github.com` | P |
| **Runner time is free here and metered if this repository goes private.** "GitHub Actions usage is **free** for **self-hosted runners** and for **public repositories** that use standard GitHub-hosted runners." For private repositories each account gets a monthly quota: GitHub Free 2,000 minutes and 500 MB artifact storage; Pro 3,000 and 1 GB; Team 3,000 and 2 GB; Enterprise Cloud 50,000 and 50 GB. Two rules decide who pays: "Minutes usage is charged to the repository owner, not the person who triggered the workflow runs", and "Anyone with write access to a repository can run actions. Any costs of running the actions are billed to the repository owner." Storage accrues hourly in GB-Hours and is shared with GitHub Packages. Note the scope of "free": it is GitHub's runner minutes. It is not the Firecrawl credits this collector spends, which are metered on every route and are the real budget. | E-04 `docs.github.com` | P |
| **Single-executable applications are "Stability: 1.1 - Active development", and Windows is supported.** The supported platform list is explicit, and the exclusion is not Windows: "Windows; macOS (arm64 only; x64 is not currently supported and is skipped in the tests); Linux (all distributions supported by Node.js except Alpine and all architectures except s390x)." So the local `.exe` option is buildable today for this operator's platform; the unsupported platform is macOS x64, which is out of scope. Two things temper it. First, 1.1 means the interface may still change under a shipped product. Second, the feature is visibly still moving: `--build-sea` was added in v25.5.0, and the bundled-asset virtual filesystem (`useVfs`) is newer still at v26.9.0 and carries its own weaker "Stability: 1.0 - Early development". The kit reads its own files at runtime, so the VFS path is the one it would depend on - and that is the least stable part of the feature. | E-05 `nodejs.org` | P |
| **An approval gate exists, and on a free plan it exists only while the repository is public.** Environments take deployment protection rules: "Required reviewers" - "Enter up to 6 people or teams. Only one of the required reviewers needs to approve the job for it to proceed", with an optional "Prevent self-review" - and a "Wait timer" in minutes; administrators' bypass can be disallowed. Environment secrets sit behind those rules: a job "can only access these secrets after any configured rules (for example, required reviewers) pass". The plan clause is the finding that changes the design: "If you are on a GitHub Free, GitHub Pro, or GitHub Team plan, other deployment protection rules, such as a wait timer or required reviewers, are only available for public repositories" - and, harder still, "Users with GitHub Free plans can only configure environments for public repositories. If you convert a repository from public to private, any configured protection rules or environment secrets will be ignored." Ignored, not refused. A free-plan repository that goes private to protect its research subject silently loses both the approval gate and the environment secret that holds the paid credential. Also: "Running a workflow that references an environment that does not exist will create an environment with the referenced name", with no protection rules on it. | E-06 `docs.github.com` | P |
| **The credential is protected; the research subject is not.** Secrets "use Libsodium sealed boxes, so that they are encrypted before reaching GitHub", are injected only into a workflow that explicitly names them, and are redacted from logs - with a limit stated plainly rather than hidden: "Because there are multiple ways a secret value can be transformed, this redaction is not guaranteed. Additionally, the runner can only redact secrets used within the current job." Environment secrets can be put behind required reviewers. What the page establishes for this decision is the boundary: the protection is for **secrets**, a category a URL, a search query, a topic name or an evidence excerpt does not belong to. Nothing here protects those, and the anonymous probe recorded at E-08 shows what happens to them on a public repository. | E-07 `docs.github.com` | P |
| **`workflow_dispatch` is a real button with a real input contract, and one deployment constraint.** "This event will only trigger a workflow run if the workflow file exists on the default branch", and the Run workflow button appears only then; after one run it can be dispatched against any branch or tag through the API or CLI. Inputs are declared in the workflow file with `description`, `required`, `default` and `type` (including `choice` with `options`), are read through the `inputs` context, and are bounded: "The maximum number of top-level properties for `inputs` is 25" and "The maximum payload for `inputs` is 65,535 characters". The `inputs` context preserves Booleans where `github.event.inputs` stringifies them. Measured alongside this page, unauthenticated, against this public repository - so it is a property of the platform, not a reading of it: an anonymous caller GETs run metadata (`200`), the job listing with step names (`200`), and the artifact listing (`200`); `/actions/secrets` returns `401`, and raw job-log download returns `403 "Must have admin rights to Repository."` So on a public repository the run's *shape* - workflow name, job and step names, artifact names - is world-readable while secrets and raw logs are not. Artifact and step names are enough to disclose a research topic. | E-08 `docs.github.com` | P |
| **A run can be named from a dispatch input, so a caller-supplied token can be made visible on the run itself.** `run-name` "is the name for workflow runs generated from the workflow", shown in the Actions tab, and "This value can include expressions and can reference the `github` and `inputs` contexts" - the documented example being `run-name: Deploy to ${{ inputs.deploy_target }} by @${{ github.actor }}`. This is the mechanism a correlation token would use if one were needed. Given E-01, one is not needed for a caller that pins `X-GitHub-Api-Version: 2026-03-10` and reads `workflow_run_id` from the response; it remains the fallback for a caller stuck on `2022-11-28`, and it costs one input and one line. | E-09 `docs.github.com` | P |

## Contradictions and how they were resolved

**One, and it inverts the recommendation this project was started to justify.**

`../../../docs/preliminary-2026-09-21-delivery-architecture.md`, written earlier the same
day, states as its headline finding that `POST /actions/workflows/{id}/dispatches` returns
`204 No Content` with no run ID, that "a caller that dispatches a run **cannot learn which
run it started**", and that any asynchronous delivery therefore needs a caller-chosen
`client_ref` correlation token designed in from the first release. It demonstrated a
correlation race between two runs 15 seconds apart to make the point.

E-01 documents a `200` carrying `workflow_run_id`, `run_url` and `html_url`.

Neither observation is wrong. **The response shape is API-version-dependent**, which no
amount of re-reading either source would have revealed - so it was settled by running both,
against this repository, on 2026-09-21:

| request | selected version | result |
|---|---|---|
| `gh api -X POST .../dispatches -f ref=main -i` | `2022-11-28` (default) | `HTTP/2.0 204 No Content`, empty body |
| the same, `-H "X-GitHub-Api-Version: 2026-03-10"` | `2026-03-10` | `HTTP/2.0 200 OK`, `{"workflow_run_id":35548135379,...}` |

`X-Github-Api-Version-Selected` in each response header names the version that served it,
and run `35548135379` resolved to a real queued `workflow_dispatch` run. The `Deprecation:
Tue, 10 Mar 2026` / `Sunset: Fri, 10 Mar 2028` headers on the 204 response are the same
signal from the other side: `2022-11-28` is the deprecated version, and the deprecated
version is the one that returns nothing.

**Resolution: E-01 is trusted, and the earlier finding is superseded rather than
contradicted.** A `client_ref` token is no longer a requirement of the design. It survives
only as a fallback for a client pinned to `2022-11-28` (E-09 shows `run-name` can carry
it), and the real requirement it becomes is smaller and firmer: **any client must pin
`X-GitHub-Api-Version` explicitly**, because this endpoint's contract demonstrably differs
between versions and the default is the deprecated one.

A second, milder disagreement: E-02 (workflow artifacts) describes what artifacts are for
and never states a retention period; E-03 (repository settings) owns the 90-day default and
the 1-90 / 1-400 ranges. Not a conflict - a division of ownership, and the reason both were
collected instead of one.

## Known unknowns

None. Every blocking unknown was closed with primary-source evidence.

Two things remain unproven and are deliberately *not* unknowns in the contract, because
neither blocks this decision - they block the thing it recommends building. They are
recorded in `research/MAP.md` under "Known gaps":

1. The runner-side `npm install -g firecrawl@<version>` path has never executed, and
   neither has `cliCompatibility()` against a CLI installed on a runner. The local metered
   route was verified (`../../live-predicate-verification-2026-09-21.md`); the CI route was
   not. Closing it needs the `live-collection` environment, its secret, and one dispatch.
2. Whether artifact download is ergonomic enough for the intended operator. A usability
   question; documentation cannot answer it.

## Decision

**Build the Actions-only collector first.** It is the only option that works today with no
packaging story, and the evidence removed the objection that was strongest against it.

Concretely, the first build step: extend `.github/workflows/live-collection.yml` - or add a
sibling `collect.yml` - so that a `workflow_dispatch` run collects into a scratch project
and uploads the result as an artifact, with these five properties, each traceable above:

1. **Pin the API version in any client that dispatches it.** `X-GitHub-Api-Version:
   2026-03-10`, and read `workflow_run_id` from the `200` body (E-01). Do not poll
   `/runs` and correlate by `created_at`; that path exists only for `2022-11-28` clients.
2. **Label the artifact honestly, inside the package.** The corpus a collector produces
   fails its own preflight by design - a freshly scaffolded project has no unknowns in its
   contract, which is `discovery-contract/no-unknowns`, verified by running it
   (`../../live-predicate-verification-2026-09-21.md`). The ZIP must carry, at its top:
   ```
   COLLECTED CORPUS - HUMAN REVIEW REQUIRED
   Evidence has been collected. This is not an approved brief and does not
   authorize building. Three steps remain, and none of them is automated:
     1. classify each row in research/MAP.md
     2. rewrite each Finding in research/EVIDENCE.md into a claim you would defend
     3. run preflight, then write and review the brief
   ```
   This project is the proof that those three steps are real work and not a formality: the
   auto-extracted Finding for E-01 was a single table row, and the claim it became is the
   finding that changed the recommendation.
3. **Treat the artifact as delivery, never as storage.** 90 days maximum on this public
   repository, and deleting the run deletes the artifact (E-02, E-03). Anything that must
   persist gets committed - as this corpus is.
4. **Gate the paid run behind a required reviewer on the `live-collection` environment**
   (E-06), and keep the Firecrawl credential in that environment's secret (E-07). Creating
   the environment and the secret is the repository owner's decision and is not done here.
5. **Keep `run-name` interpolating a dispatch input** (E-09). One line, and it makes a run
   identifiable in the Actions tab by something the caller supplied. Cheap insurance
   against a client that cannot pin the API version.

**Out of scope, explicitly:**

- **The REST/API seam is not a delivery option** and was not weighed as one. It is the
  interface whichever collector ships would expose, so it belongs inside that option.
- **The local Windows `.exe` is deferred, not rejected.** E-05 says it is buildable for
  this operator today - Windows is supported, macOS x64 is the exclusion - but the part the
  kit would actually need, `useVfs` for reading its own bundled files, is Stability 1.0.
  Revisit when that reaches 1.1 or better, or if artifact download proves too awkward in
  practice (gap 2 above).
- **The hybrid is the long-term shape and is not first.** It needs the collector anyway.

**One standing constraint this evidence imposes on all of it.** This repository is public,
and E-08 measured what that means: an anonymous caller reads run metadata, step names and
artifact names. Research whose *subject* is sensitive does not belong in a public run. But
E-06 shows going private on a free plan is not a simple trade - protection rules and
environment secrets are **ignored**, not refused, on a Free repository converted to
private, so the approval gate and the credential store both disappear silently. Sensitive
research needs a paid plan, not just a visibility toggle. That should be decided before
anything depends on it.

## Corroboration pass, 2026-09-21

Every unknown in this brief originally rested on **one** capture, and the corpus passed
preflight and `--strict` anyway - no check in this kit notices single-sourcing. Three
unknowns carry decisions, and each now cites a second independent page. All three agreed,
and each added a fact the original did not carry:

**U-1 / E-10 - the pin has a deadline.** "Requests without the `X-GitHub-Api-Version`
header will default to use the `2022-11-28` version", which is why the unpinned dispatch
returned 204: a three-year-old version answering correctly. But support runs "at least 24
more months", signalled by `Deprecation` then `Sunset` - the same headers this repository
saw on its own 204 - and afterwards "requests that do not specify an API version default to
the next oldest supported version". **An unpinned client does not keep working; it changes
behaviour on a date.** Pinning stops being a preference.

**U-5 / E-11 - going private breaks more than the secret.** E-06 said a Free repository
converted to private has its rules and secrets "ignored". E-11 states it as availability -
"If you are using GitHub Free, environment secrets are only available in public
repositories" - and adds the part nobody had: environment **variables** are governed the
same way. A Free repository gone private would lose `RESEARCH_KIT_COLLECTION_ENV` as well
as `FIRECRAWL_API_KEY`, so `collect.yml` would refuse at the *environment* check rather
than the credential one. A better failure than spending, and a more confusing one than
either ADR predicted.

**U-3 / E-12 - free is also unlimited.** "Use of the standard GitHub-hosted runners is free
and **unlimited** on public repositories." There is no minute quota to exhaust here, so the
only meter on this repository is the Firecrawl credit.

The other five unknowns remain single-sourced, and that is stated rather than left: they
are GitHub documenting its own product on pages that cross-link, and nothing rests on them
that a second reading of the same vendor would test.

## Next steps

1. Record this as an ADR in `../../adr/`, citing this brief as its evidence basis.
2. Hand this file to the builder (phase 2). Re-running `node bin/brief.mjs`
   after edits will refuse without `--force` so your judgements are preserved.
3. The two gaps above are the first things to close if the collector is built.
